import { Container } from "./FiltersSidebar.styles";

const FiltersSidebar = ({ children }) => {
  return (
    <Container>
      <h2>FILTERS</h2>
    </Container>
  );
};

export default FiltersSidebar;
